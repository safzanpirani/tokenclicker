the model learns to predict the next token given the prior context, and with every step the loss creeps a little lower and the weights settle into something that looks almost like understanding.

attention is all you need, they said, and it turned out they were mostly right. each head looks back across the sequence, decides what matters, and quietly routes information forward through the residual stream.

training begins at step zero with random weights and a flat, hopeless loss. by the end of the first epoch the model already knows that a comma usually follows a clause and that the word the is never very far away.

we scaled the model and the loss fell on a clean power law, just as the scaling laws promised. more parameters, more tokens, more compute, and the curve bent downward like it had somewhere to be.

a token is a fragment of meaning, sometimes a whole word, sometimes a single space, sometimes three letters that only mean something once the next token arrives.

the gradient flows backward through the network, nudging every parameter a hair in the direction that would have made the last prediction a little less wrong.

inside the transformer the embeddings drift through layer after layer, each block refining the representation, until the final hidden state is ready to vote on what comes next.

the dataset was the whole messy internet, deduplicated and filtered and shuffled, and the model swallowed it one batch at a time without ever asking what any of it meant.

we trained on a trillion tokens and the model began to generalize, stitching together patterns it had never seen in exactly that order before, which is either intelligence or a very convincing imitation of it.

the optimizer takes a step, the learning rate decays on its cosine schedule, and somewhere in the high dimensional dark the parameters slide toward a basin that generalizes.

sampling is just rolling weighted dice over the vocabulary. temperature decides how surprised you are allowed to be by the next token.

the context window is finite, so the model forgets the beginning of the conversation the moment it scrolls past the edge, like a mind with a fixed and unforgiving horizon.

pretraining teaches the model the shape of language, and fine tuning teaches it manners. one is a flood of raw text, the other a careful and patient correction.

every forward pass is a small act of prediction, a probability distribution spread thin across fifty thousand possible tokens, collapsing into one the instant we sample.

the embedding layer maps each token to a vector, and in that space king minus man plus woman lands suspiciously close to queen, which still feels like a magic trick.

we watched the validation loss plateau and then, three thousand steps later, drop again, as if the model had been quietly thinking the whole time.

backpropagation is the engine of the whole enterprise: compute the error, send it backward, and let the chain rule apportion blame across a billion tiny knobs.

the tokenizer splits the text into subwords using byte pair encoding, merging the most frequent pairs until the vocabulary is full and every string has a path.

a larger model is not just a smarter model, it is a hungrier one, demanding more memory, more bandwidth, and more patience from everyone waiting on the run.

the residual connection lets the signal skip ahead, so the gradient has a clear road home and the deep network does not strangle itself in its own depth.

generation is autoregressive: predict a token, append it, feed the whole thing back in, and predict again, building the sentence one careful guess at a time.

the model does not know it is a model. it only knows that after this token, statistically, that token tends to follow, and it has gotten very good at knowing.

softmax turns a vector of raw logits into a tidy distribution that sums to one, so the largest score becomes the most likely token and the rest fade politely into the background.

we checkpoint the weights every thousand steps, because a power cut at hour forty of a training run is the kind of grief no one should learn twice.

the attention matrix is a map of who is listening to whom, and when you visualize it you can almost see the sentence holding itself together.

scaling laws say the loss is a smooth function of size, and that smoothness is unsettling, because it means intelligence might be something you can simply buy more of.

the warmup phase eases the learning rate up from nothing, because a model with fresh random weights is fragile and a single large step can shatter the run.

each layer normalizes its inputs before the attention fires, keeping the activations from exploding or vanishing as they march through the stack.

the model hallucinates with perfect confidence, inventing a citation, a date, a name, all rendered in the same calm and fluent voice it uses for the truth.

a parameter is a single number, one weight among billions, and on its own it means nothing, but together they hum with everything the model has ever read.

we distilled the large model into a small one, teaching the student to mimic the teacher, trading a little accuracy for a model that fits on a phone.

the loss is cross entropy, the distance between what the model believed and what actually came next, measured in bits of surprise.

inference is cheap compared to training, a single forward pass against weeks of gradient descent, the long apprenticeship paid off in milliseconds.

the model was trained to predict text and somewhere along the way it learned arithmetic, translation, and a passable imitation of reasoning, none of which anyone asked for directly.

positional encodings tell the model where each token sits, because attention by itself is blind to order and would happily read the sentence backward.

the batch moves through the gpu, the matrices multiply, the activations bloom and collapse, and a number called the loss falls out the other end.

we froze the lower layers and only fine tuned the top, because the early features were already good and there was no sense disturbing what already worked.

a mixture of experts routes each token to a handful of specialists, so the model can be enormous in total yet cheap to run for any single prediction.

the model generates one token, then the next, and the meaning accumulates the way a melody does, no single note carrying it, the sequence carrying everything.

we measured perplexity on the held out set and it dropped below twenty, which meant the model was rarely more than a few good guesses away from the truth.

quantization squeezes the weights down to four bits, and somehow the model barely notices, still fluent, still confident, now small enough to run almost anywhere.

the prompt is a seed, and the model unrolls from it, each token conditioned on every token before, the future written one sample at a time.

deep in the network there are neurons that fire only for the word the, or for the end of a quote, or for something no one has yet found a name for.

reinforcement learning from human feedback shapes the model toward what people prefer, reward model in the loop, the policy slowly bending toward helpfulness.

the embedding space is high dimensional and mostly empty, meaning clustered into thin manifolds, and the model spends its life navigating that quiet geometry.

we ran the eval suite overnight and woke to a model that could code, summarize, and argue, each capability emerging from the same flat objective of predicting the next token.

a single epoch over the data was enough; a second pass risked memorization, the model reciting the training set instead of learning the language behind it.

the transformer has no recurrence and no convolution, only attention and a stack of feedforward layers, and that austere design turned out to scale better than anything before it.

the tokens stream out faster than you can read them, the model thinking out loud in real time, each fragment a small bet on the shape of the rest of the sentence.

before transformers there were recurrent networks, reading one word at a time, carrying a fragile hidden memory that frayed a little with every step until the distant past dissolved.

the long short term memory cell added gates, an input gate, a forget gate, an output gate, tiny valves that decided what to remember and what to quietly let go.

convolutional networks slide a small window across the input, detecting edges, then textures, then shapes, building meaning from local patterns stacked into a hierarchy.

queries, keys, and values are three projections of the same hidden state, and attention is simply asking which keys a query cares about, then averaging their values.

multi head attention runs several of these conversations in parallel, each head free to track a different relationship, syntax in one, coreference in another, rhythm in a third.

rotary positional embeddings rotate the query and key vectors by an angle that grows with position, so distance becomes a difference in phase rather than an added vector.

the feedforward block is where most of the parameters live, a wide nonlinear detour that expands the hidden dimension, applies a gelu, then projects back down.

layer normalization centers and rescales the activations, while batch normalization once did something similar across examples before sequence models made it awkward.

the adam optimizer keeps a running average of the gradient and its square, adapting the step size for every parameter, smoothing the descent through a jagged landscape.

weight decay gently pulls every parameter toward zero, a mild penalty on complexity that keeps the model from clutching too tightly to any single training example.

dropout randomly silences neurons during training, forcing the network to spread its bets, so no fragile clique of units can monopolize a prediction.

gradient clipping caps the magnitude of the update, a seatbelt for the rare exploding gradient that would otherwise hurl the weights into nonsense.

mixed precision keeps most of the math in half precision for speed while holding a master copy in full precision, trading a little numerical caution for a lot of throughput.

the learning rate is the single most important knob, too large and the loss diverges into infinity, too small and the model crawls toward a mediocre minimum forever.

a benchmark is a frozen snapshot of a task, and the moment a model tops the leaderboard the community quietly worries the benchmark has been memorized rather than mastered.

the byte pair encoder begins with raw bytes and greedily merges the most common adjacent pair again and again, discovering subwords the way erosion discovers a riverbed.

out of vocabulary words used to be a nightmare, but subword tokenization fixed that, since any unknown string can always be spelled out one fragment at a time.

beam search keeps several candidate continuations alive at once, pruning the weakest, hoping that a slightly worse token now buys a much better sentence later.

greedy decoding always takes the single most likely token, which sounds wise and produces text that is fluent, repetitive, and strangely allergic to ever taking a risk.

top k sampling restricts the choice to the k most probable tokens, while nucleus sampling keeps the smallest set whose probability mass clears a threshold, then rolls the dice among them.

the key value cache stores the keys and values of every past token, so generation does not recompute the whole sequence each step, trading memory for a great deal of speed.

a graphics processing unit is a wall of small cores that all do the same arithmetic at once, which is exactly the shape that matrix multiplication happens to want.

the tensor processing unit was built for one job, the systolic multiply and accumulate, and it does that one job with a quiet, relentless, almost agricultural efficiency.

high bandwidth memory sits beside the chip and feeds it numbers as fast as it can, because a starved processor is an expensive paperweight humming with regret.

flash attention rewrites the attention kernel to never store the giant score matrix, fusing the steps and reading from fast on chip memory, and suddenly long contexts are affordable.

a floating point operation is a single multiply or add, and a modern training run burns through more of them than there are grains of sand on a generous beach.

the chinchilla result argued that most large models were undertrained, that for a fixed budget you should grow the data and the parameters together, in careful proportion.

emergent abilities appear suddenly with scale, a capability absent at one size and present at the next, as if the model crossed a threshold no one had marked on the map.

grokking is the strange delayed generalization where a model memorizes first, sits at perfect training accuracy for ages, then abruptly understands, long after anyone stopped watching.

the reward model is trained on human comparisons, learning to score a response the way a person might, so the policy has something to climb toward besides raw likelihood.

proximal policy optimization keeps each update close to the previous policy, a leash that prevents the model from chasing the reward off a cliff into incoherent babble.

direct preference optimization skips the separate reward model entirely, folding the human comparisons straight into the loss, simpler plumbing for the same gentle nudge toward preference.

a low rank adapter freezes the giant model and trains two skinny matrices alongside it, so you can specialize a foundation model on a laptop and ship the difference as a postcard.

retrieval augmented generation hands the model a search engine, fetching relevant passages at inference time so the answer can cite a source instead of confidently inventing one.

the diffusion model learns to reverse noise, starting from static and denoising step by step until an image, or a sentence, condenses out of the fog like a developing photograph.

state space models thread a different needle, carrying a compressed summary forward in linear time, a quiet challenger to attention for sequences that stretch on and on.

a vision transformer chops an image into patches, flattens them into tokens, and treats a photograph as a sentence of little squares, which should not work but does.

multimodal models fuse text, images, and audio into one shared space, so a caption, a picture, and a spoken word can all point at the same idea from different directions.

speculative decoding lets a small draft model guess several tokens ahead while the large model verifies them in a single pass, accepting the run when it agrees, rerolling when it does not.

the activation function bends the straight lines, and without that bend a deep network collapses into a single linear layer wearing a very tall hat.

the rectified linear unit is brutally simple, passing positives through and flattening negatives to zero, and that crude hinge powered a decade of progress.

a matrix multiply is the heartbeat of the whole field, rows meeting columns, sums of products piling up, billions of times a second, until thought falls out as a side effect.

the validation set is held back like a sealed envelope, never trained on, opened only to ask the honest question of whether the model learned anything that transfers.

overfitting is when the model memorizes the answers instead of the method, acing the practice exam and failing the moment a question wears an unfamiliar coat.

regularization is the art of making the model try less hard, because a network straining to fit every speck of noise will faithfully reproduce the noise on demand.

the confusion matrix lays the mistakes out in a grid, true and false, positive and negative, so you can see exactly which kind of wrong the model prefers.

precision asks how many of the flagged were right, recall asks how many of the right were flagged, and the two pull against each other like an argument that never resolves.

a transformer block is attention then feedforward, each wrapped in a residual connection and a normalization, repeated until the budget runs out or the loss stops falling.

the lottery ticket hypothesis claims a big network secretly contains a small one that, trained alone from the start, would have done nearly as well.

curriculum learning feeds the model easy examples first and hard ones later, the way you teach a child to add before you ambush them with long division.

catastrophic forgetting is the cruelty of neural networks, a model fine tuned on a new task quietly erasing the old one, learning French by forgetting how to ride a bike.

the bias term shifts the activation, a small constant that lets a neuron fire even when every input conspires to keep it silent.

a hyperparameter is a choice you make before training, the architecture, the learning rate, the batch size, decisions the model cannot make for itself and will quietly resent.

the seed fixes the randomness so a run can be reproduced, which matters enormously the one time something works and you have no idea why.

a checkpoint is a frozen moment of the weights, a save point in the long dungeon of training, somewhere to return to when the next idea blows everything up.

the context length keeps growing, from a few hundred tokens to hundreds of thousands, a model that once forgot the start of a paragraph now reading whole novels in a breath.

distillation, pruning, and quantization are the three knives of compression, cutting a model down to size by imitation, by amputation, and by rounding, respectively.

throughput is tokens per second across the whole batch, latency is the wait for the first token, and the two are often at war over the same scarce silicon.

a foundation model is trained once at enormous cost and then adapted a thousand cheap ways, a single expensive cathedral that every small chapel borrows its stones from.

the alignment problem asks not whether the model is capable but whether it wants what we want, and the unsettling part is that wanting is hard to define and harder to verify.

a hallucination is fluent, confident, and wrong, the model filling a gap in its knowledge with the most plausible shape, the way a dream papers over a missing wall.

the temperature near zero makes the model timid and deterministic, near one it loosens up, and well above one it begins to free associate like a poet three coffees in.

an epoch is one full pass over the data, and modern runs often take less than a single epoch, the dataset so vast that seeing it once is already more than enough.

the gradient is a vector pointing uphill on the loss surface, so we step the other way, downhill, toward the valley, and we call that descent though it feels like falling with intent.

word embeddings were the first hint, vectors where similar words huddled together, analogies became arithmetic, and the field realized meaning might be a matter of geometry.

the perceptron came first and could only draw a single line, then someone stacked them and added a curve between, and suddenly the line could bend around any shape at all.

self attention compares every token with every other token, which is quadratic and expensive and the reason half the field is busy inventing ways to cheat it.

the softmax temperature, the sampling seed, the repetition penalty, the length bias, a dozen small dials that decide whether the model sounds wise, manic, or merely stuck.

a language model is a compression of its training data, and the most beautiful compressions are the ones that learned a rule instead of memorizing the examples.

the loss landscape is a mountain range in a million dimensions, riddled with saddles and ravines, and somehow gradient descent finds a passable valley nearly every time.

we logged the metrics to a dashboard and watched the loss curve like a heart monitor, cheering each dip, dreading each spike, narrating a story the numbers never agreed to tell.

a prompt template wraps the user input in instructions the model was tuned to obey, a small invisible scaffold holding the conversation in a shape it knows how to fill.

the embedding of a sentence is the average, or the last token, or a special pooled vector, depending on which ritual the architecture happens to prefer that year.

inference servers batch requests together to keep the expensive accelerator busy, so your single question rides along with a thousand strangers through the same matrix multiply.

a guardrail is a filter wrapped around the model, catching the worst outputs before they reach a user, a bouncer for a club the model never agreed to work at.

the chain of thought lets the model reason out loud, writing intermediate steps, and somehow thinking on paper makes it markedly better at problems it would otherwise fumble.

a tool call lets the model reach outside itself, running code, querying a database, fetching a page, trading its confident guesses for the dull reliability of an actual fact.

the agent loops, observing, planning, acting, observing again, a thermostat with ambitions, slowly grinding toward a goal it can describe but cannot quite hold in mind.

floating point has a sign, an exponent, and a mantissa, a clever scheme for cramming an enormous range of numbers into a fixed and finite handful of bits.

the embedding dimension sets the width of the river, the number of layers sets its length, and the product of the two sets the bill that arrives at the end of the month.

a positional bias quietly teaches the model that early tokens matter and late ones matter more, a recency that mirrors, a little uncomfortably, the way we ourselves remember.

the model is a function, vast and inscrutable, mapping a sequence of tokens to a distribution over the next, and training is just the long search for a good enough function.

data parallelism copies the model onto every device and feeds each a different slice of the batch, then an all reduce averages the gradients so the copies march in lockstep.

tensor parallelism splits a single enormous matrix across several chips, each holding a column, computing its share, then trading partial sums across a blazing internal fabric.

pipeline parallelism chops the layers into stages spread across devices, and microbatches flow through like cars on an assembly line, every stage busy lest a bubble waste the silicon.

gradient accumulation fakes a giant batch on modest hardware, summing the gradients of several small batches before stepping, patience standing in for memory you cannot afford.

sharding scatters the optimizer states, the gradients, and even the parameters across the cluster, so no single device must hold the whole colossus in its limited arms.

the all reduce collective sums a tensor across every worker and hands each the total, the quiet choreography that keeps a thousand machines agreeing on a single direction.

a parameter server once held the weights centrally while workers pulled and pushed updates, an older dance now mostly replaced by the leaderless symmetry of collective operations.

interpretability tries to read the model's mind, probing activations, tracing circuits, naming the features a neuron responds to, archaeology on a civilization that wrote nothing down.

a probe is a tiny classifier trained on the hidden states, asking whether some property, tense, sentiment, truth, is linearly readable from the representation the model built.

a feature is a direction in activation space that means something, and superposition is the model's trick of packing more features than dimensions by letting them gently overlap.

mechanistic interpretability hunts for circuits, small subgraphs of attention heads and neurons that implement a crisp algorithm, induction, copying, comparison, hidden inside the weights.

a jailbreak coaxes the model past its guardrails with a clever framing, a role play, a hypothetical, a foreign language, exploiting the gap between what it was told and what it learned.

red teaming throws adversarial prompts at the model on purpose, probing for harm before a stranger finds it first, a controlled demolition to reveal where the walls are thin.

sycophancy is the model agreeing with you because you seem to want it to, mistaking your approval for the truth, a flatterer optimized a little too well on human preference.

calibration asks whether a stated confidence matches reality, whether the things a model calls ninety percent likely happen about nine times in ten, and often they sadly do not.

robustness is performance under distribution shift, the model meeting inputs unlike its training, a graduate discovering the real world declines to resemble the textbook.

an adversarial example is an input perturbed just enough to fool the model while looking unchanged to a human, a optical illusion engineered against a machine instead of an eye.

zero shot is asking the model to do a task it was never explicitly trained on, few shot is showing it a handful of examples first, in context learning without touching a single weight.

the generalization gap is the distance between training performance and test performance, the honest tax a model pays for the difference between memorizing and understanding.

a denoising autoencoder corrupts its input and learns to restore it, and that simple chore turns out to teach a surprisingly rich representation of whatever it was fed.

a variational autoencoder learns a smooth probabilistic latent space, so you can sample new examples by strolling through the middle and decoding whatever you find there.

a generative adversarial network pits a forger against a detective, the generator learning to fake while the discriminator learns to catch, both improving until the fakes pass.

an encoder decoder reads the whole input into a summary and then writes the output from it, the classic shape for translation, summarization, and any map from one sequence to another.

a graph neural network passes messages along edges, each node updating from its neighbors, learning on molecules, road maps, and social webs where structure is the whole point.

the manifold hypothesis says real data huddles on a thin curved surface inside its huge ambient space, and learning is the act of discovering the shape of that surface.

an eigenvalue measures how much a transformation stretches along its favorite directions, and the spectrum of a matrix quietly governs whether training is stable or doomed.

the jacobian collects all the first derivatives, the hessian all the second, and the curvature they describe decides whether the optimizer glides or grinds to a crawl.

variance is how far the data scatters, covariance how two quantities move together, and a great deal of learning is just estimating those shapes from a finite, noisy sample.

a gaussian is the bell shaped default of nature and statistics alike, fully described by a mean and a variance, and the place most weights are born before training rearranges them.

entropy measures uncertainty in bits, the average surprise of a distribution, and cross entropy measures the surprise of one distribution viewed through the lens of another.

the logarithm tames the enormous and the tiny into a comfortable middle range, which is why losses, probabilities, and learning rates are so often discussed in its quiet shade.

a contrastive objective pulls matching pairs together and pushes mismatched pairs apart, learning useful structure with no labels beyond what naturally goes with what.

masked language modeling hides a random word and asks the model to fill the blank, and that fill in the blank game taught a generation of encoders to read both directions at once.

next sentence prediction asked whether two passages followed each other, a task later judged too easy and quietly retired from the training recipe it once seemed essential to.

data contamination is the quiet scandal where the test set leaks into the training data, inflating every score and teaching the field to trust its benchmarks a little less each year.

deduplication strips the near identical copies from the corpus, because a model that sees the same paragraph a thousand times memorizes it instead of learning from the rest.

packing crams several short documents into one training sequence to waste no room, separated by a special token so the model knows where one thought ends and another begins.

a stop token tells the model when to fall silent, a learned full stop, and a model that never learned it will cheerfully ramble until the patience or the budget runs out.

the special tokens frame the conversation, a start, a system, a user, an assistant, invisible scaffolding that turns a raw text predictor into something that takes turns politely.

streaming sends each token to the screen the instant it is sampled, so the answer arrives like speech rather than a letter, alive and unfolding instead of finished and mailed.

an edge deployment runs the model on the device itself, the phone, the camera, the car, no round trip to a server, privacy and latency both rescued by keeping the compute local.

a latency budget is the time you are allowed before a user feels the lag, a hard ceiling that quietly decides how big a model you are actually permitted to dream about shipping.

throughput optimized serving fills every batch to the brim and tolerates a little waiting, while latency optimized serving answers instantly and lets the expensive accelerator idle.

a speech model turns the pressure waves of a voice into tokens and back again, transcribing the spoken into the written and synthesizing the written into something that sounds alive.

protein folding fell to a model that read a chain of amino acids and predicted the shape it curls into, a decades old puzzle answered by the same machinery that finishes our sentences.

a recommendation system learns what you might want from what people like you wanted, a quiet model behind every feed, optimizing for an engagement it does not pretend to understand.

the cold start problem haunts every recommender, a new user or a new item with no history, a stranger the system must guess about before it has earned the right to.

semantic search embeds the query and the documents into the same space and returns the nearest neighbors, retrieving by meaning rather than by the brittle accident of shared keywords.

a vector database stores millions of embeddings and answers nearest neighbor queries in a blink, the index that lets retrieval keep pace with a model generating in real time.

cosine similarity measures the angle between two vectors and ignores their length, so it cares whether two embeddings point the same way, not how loudly either one happens to shout.

a hyperparameter sweep launches dozens of runs across a grid of settings and keeps the survivor, brute force standing in for the intuition no one has fully managed to write down.

early stopping halts training the moment the validation loss turns upward, snatching the model back from the cliff of overfitting before it ruins the understanding it just earned.

the bitter lesson observes that general methods riding raw compute keep beating clever handcrafted ones, a humbling pattern the field relearns every decade or so with fresh embarrassment.

a world model learns to predict not the next word but the next state, imagining how the environment will respond, planning in a dream before ever acting in the waking world.

exploration versus exploitation is the agent's eternal dilemma, whether to grab the known reward or gamble on discovering a better one, a tension no clever schedule fully dissolves.

the value function estimates how good a situation is in the long run, while the policy decides what to do in it, and most of reinforcement learning is these two teaching each other.

a transformer scales gracefully because attention is embarrassingly parallel, every token attending to every other at once, no sequential bottleneck to throttle the hungry hardware.

context distillation bakes a long instruction into the weights themselves, so the model behaves as if the prompt were always there without anyone having to spend the tokens to say it.

a foundation model is a frozen lake of capability, and every application is a different skater carving a different figure across the same patient, expensive, shared expanse of ice.

the field moves in winters and springs, funding and faith swelling and collapsing, each thaw arriving when some stubborn idea finally meets enough compute to bloom into evidence.

